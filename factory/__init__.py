"""AI YouTube Factory — shared core package.

Imported by the API (`backend`), the queue workers (`worker`), and the
Telegram bot (`telegram_bot`) so all three share one source of truth.
"""

__version__ = "0.1.0"
